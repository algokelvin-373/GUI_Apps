public class Main extends FrameController {
    public static void main(String[] args) {
        GUI_Statistic gui = new GUI_Statistic();
        String title = "GUI AlgoKelvin: Statistic";
        setFrameGUI(gui.getPanel(), title);
    }
}
